# ==============================================================
# Auto-Apply Backend — Development / Testing Dependencies
# ==============================================================
-r requirements.txt

# --- Testing & Coverage ---
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
pytest-benchmark==4.0.0
pytest-mock==3.12.0

# --- Linting & Formatting ---
flake8==7.1.1
black==24.10.0
isort==5.13.2

# --- Type Checking ---
mypy==1.14.1

# --- Security Testing ---
safety==3.2.11
bandit==1.7.10

# --- Development Tools ---
pre-commit==4.0.1
ipython==8.29.0

# --- Agent Evals (Phase 1+2 deterministic layer) ---
# PyYAML — already a transitive dep of FastAPI/Alembic but pinned
# explicitly so ``evals/conftest.py`` doesn't break if the transitive
# install changes.
pyyaml>=6.0,<7.0

# --- Agent Evals (Phase 3+ LLM-as-judge) — installed ON-DEMAND ---
# DeepEval is NOT installed here because every released version
# conflicts with the repo's pinned production dependencies:
#   * deepeval 2.x → requires tenacity~=8.4.1 (repo has tenacity==9.0.0)
#   * deepeval 4.x → requires pydantic>=2.11.7 (repo has pydantic==2.10.5)
# Upgrading those pins repo-wide would risk regressions in the agent
# code paths (pydantic validators changed subtly in 2.11). Instead,
# deepeval is installed only in the ``evals-llm`` CI job that needs it,
# and each ``evals/deepeval/test_*.py`` uses ``pytest.importorskip`` so
# pytest collection passes even when deepeval is absent.
#
# To run LLM-judge evals locally:
#   pip install 'deepeval>=4.0,<5.0'
